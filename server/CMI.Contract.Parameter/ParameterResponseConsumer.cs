using System;
using System.Threading.Tasks;
using MassTransit;

namespace CMI.Contract.Parameter
{
    /* public class ParameterResponseConsumer : IConsumer<ParameterResponse>
    {
        public async Task Consume(ConsumeContext<ParameterResponse> context)
        {
            await Console.Out.WriteLineAsync(context.Message.Message);
        }
    }*/
}
